let wight = parseInt(prompt('Enter the width'))
        let height = parseInt(prompt('Enter the height'))
        let area = wight * height
        document.write('Enter width ' + wight)
        document.write('<br>')
        document.write('Enter height ' + height)
        document.write('<br>')
        document.write('The area is ' + area)